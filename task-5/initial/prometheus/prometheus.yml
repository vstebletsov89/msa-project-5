global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "stock-etl-batch"
    metrics_path: "/actuator/prometheus"

    static_configs:
      - targets: ["stock-etl-app:8080"]
        labels:
          application: stock-etl-batch
          environment: local